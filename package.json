{
  "name": "sleepwalker",
  "version": "1.0.0",
  "license": "ISC",
  "scripts": {
    "setup": "yarn setup-clients && yarn setup-core",
    "setup-clients": "yarn setup-clients-simple && yarn setup-clients-puppeteer",
    "setup-clients-simple": "cd clients/simple && yarn",
    "setup-clients-puppeteer": "cd clients/puppeteer && yarn && yarn install-chrome",
    "setup-core": "yarn setup-konsole && yarn setup-wrapper && yarn setup-armada && yarn setup-service",
    "setup-konsole": "cd core/konsole && yarn",
    "setup-wrapper": "cd core/wrapper && yarn",
    "setup-armada": "cd core/armada && yarn",
    "setup-service": "cd core/service && yarn",
    "wrapper": "cd core/wrapper && yarn run start && tail -0f wrapper.log",
    "start": "cd core/service && yarn run start && tail -0f armada.log",
    "clean": "yarn clean-simple && yarn clean-puppeteer && yarn clean-armada && yarn clean-konsole && yarn clean-wrapper && yarn clean-service",
    "clean-simple": "cd clients/simple && yarn clean",
    "clean-puppeteer": "cd clients/puppeteer && yarn clean",
    "clean-armada": "cd core/armada && yarn clean",
    "clean-konsole": "cd core/konsole && yarn clean",
    "clean-wrapper": "cd core/wrapper && yarn clean",
    "clean-service": "cd core/service && yarn clean",
    "tarball": "tar cvf armada.tar.xz clients/ core/ package.json"
  }

}
