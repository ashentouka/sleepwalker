{
  "name": "@sleepwalker/proxyarmada",
  "version": "1.0.0",
  "main": "core/core.js",
  "scripts": {
    "clean": "rm -rf node_modules yarn.lock asn/*.json",
    "preinstall": "cat ascii && cd asn && tar xf tar/asnmap.tar.xz && tar xf tar/asn.tar.xz",
    "testdev": "./armada.sh --minimal --debug",
    "minimal": "./armada.sh --minimal",
    "debug": "./armada.sh --debug",
    "armada": "./armada.sh"
  },
  "license": "ISC",
  "dependencies": {
    "@sleepwalker/router": "../../router/",
    "async": "^3.2.5",
    "require-json5": "^1.3.0"
  }
}
